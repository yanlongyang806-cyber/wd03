//For more info on queued commands, look here: http://crypticwiki:8081/display/Core/AUTO_COMMAND_QUEUED
//This file is autogenerated. autogeneratednocheckin
#include "commandqueue.h"

void ClearPowersOverrideFSM(Entity* e,U32 overrideFSMID);
void ClearPowersOverrideFSM_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	U32 overrideFSMID;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &overrideFSMID, sizeof(U32));

	ClearPowersOverrideFSM(e, overrideFSMID);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_ClearPowersOverrideFSM(CommandQueue *pQueue, Entity* e,U32 overrideFSMID)
{
	void *pFunc = ClearPowersOverrideFSM_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &overrideFSMID, sizeof(U32));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void ExecuteQueue(CommandQueue* queue);
void ExecuteQueue_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	CommandQueue* queue;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &queue, sizeof(void*));

	ExecuteQueue(queue);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct CommandQueue CommandQueue;

void QueuedCommand_ExecuteQueue(CommandQueue *pQueue, CommandQueue* queue)
{
	void *pFunc = ExecuteQueue_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &queue, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiAmbientExitHandler(Entity* e,AIVarsBase* aib,FSMLDAmbient* ambientData);
void aiAmbientExitHandler_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;
	FSMLDAmbient* ambientData;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));
	CommandQueue_Read(pQueue, &ambientData, sizeof(void*));

	aiAmbientExitHandler(e, aib, ambientData);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;
typedef struct FSMLDAmbient FSMLDAmbient;

void QueuedCommand_aiAmbientExitHandler(CommandQueue *pQueue, Entity* e,AIVarsBase* aib,FSMLDAmbient* ambientData)
{
	void *pFunc = aiAmbientExitHandler_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));
	CommandQueue_Write(pQueue, &ambientData, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiAnimListRemoveAnim(Entity* e,const char* animList,int handle);
void aiAnimListRemoveAnim_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	char* animList = NULL;
	int handle;

	CommandQueue_EnterCriticalSection(pQueue);
	estrStackCreate(&animList);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_ReadString(pQueue, &animList);
	CommandQueue_Read(pQueue, &handle, sizeof(int));

	aiAnimListRemoveAnim(e, animList, handle);
	estrDestroy(&animList);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_aiAnimListRemoveAnim(CommandQueue *pQueue, Entity* e,const char* animList,int handle)
{
	void *pFunc = aiAnimListRemoveAnim_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_WriteString(pQueue, animList);
	CommandQueue_Write(pQueue, &handle, sizeof(int));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiAnimListRemoveFX(Entity* e,const char* animList,const char* name,int handle);
void aiAnimListRemoveFX_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	char* animList = NULL;
	char* name = NULL;
	int handle;

	CommandQueue_EnterCriticalSection(pQueue);
	estrStackCreate(&animList);
	estrStackCreate(&name);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_ReadString(pQueue, &animList);
	CommandQueue_ReadString(pQueue, &name);
	CommandQueue_Read(pQueue, &handle, sizeof(int));

	aiAnimListRemoveFX(e, animList, name, handle);
	estrDestroy(&animList);
	estrDestroy(&name);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_aiAnimListRemoveFX(CommandQueue *pQueue, Entity* e,const char* animList,const char* name,int handle)
{
	void *pFunc = aiAnimListRemoveFX_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_WriteString(pQueue, animList);
	CommandQueue_WriteString(pQueue, name);
	CommandQueue_Write(pQueue, &handle, sizeof(int));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiAnimListRestoreDefaultStance(Entity* e,const char* animList,const char* stancePowerDefString);
void aiAnimListRestoreDefaultStance_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	char* animList = NULL;
	char* stancePowerDefString = NULL;

	CommandQueue_EnterCriticalSection(pQueue);
	estrStackCreate(&animList);
	estrStackCreate(&stancePowerDefString);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_ReadString(pQueue, &animList);
	CommandQueue_ReadString(pQueue, &stancePowerDefString);

	aiAnimListRestoreDefaultStance(e, animList, stancePowerDefString);
	estrDestroy(&animList);
	estrDestroy(&stancePowerDefString);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_aiAnimListRestoreDefaultStance(CommandQueue *pQueue, Entity* e,const char* animList,const char* stancePowerDefString)
{
	void *pFunc = aiAnimListRestoreDefaultStance_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_WriteString(pQueue, animList);
	CommandQueue_WriteString(pQueue, stancePowerDefString);

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiCivClearHostileToCivilians(Entity* e);
void aiCivClearHostileToCivilians_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));

	aiCivClearHostileToCivilians(e);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_aiCivClearHostileToCivilians(CommandQueue *pQueue, Entity* e)
{
	void *pFunc = aiCivClearHostileToCivilians_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiClearAllQueuedPowers(Entity* e,AIVarsBase* aib);
void aiClearAllQueuedPowers_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));

	aiClearAllQueuedPowers(e, aib);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiClearAllQueuedPowers(CommandQueue *pQueue, Entity* e,AIVarsBase* aib)
{
	void *pFunc = aiClearAllQueuedPowers_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiClearPreferredTarget(Entity* e);
void aiClearPreferredTarget_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));

	aiClearPreferredTarget(e);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_aiClearPreferredTarget(CommandQueue *pQueue, Entity* e)
{
	void *pFunc = aiClearPreferredTarget_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiCombatExitCombatFSM(Entity* e,AIVarsBase* aib);
void aiCombatExitCombatFSM_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));

	aiCombatExitCombatFSM(e, aib);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiCombatExitCombatFSM(CommandQueue *pQueue, Entity* e,AIVarsBase* aib)
{
	void *pFunc = aiCombatExitCombatFSM_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiCombatExitHandler(Entity* e,AIVarsBase* aib,FSMLDCombat* mydata);
void aiCombatExitHandler_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;
	FSMLDCombat* mydata;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));
	CommandQueue_Read(pQueue, &mydata, sizeof(void*));

	aiCombatExitHandler(e, aib, mydata);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;
typedef struct FSMLDCombat FSMLDCombat;

void QueuedCommand_aiCombatExitHandler(CommandQueue *pQueue, Entity* e,AIVarsBase* aib,FSMLDCombat* mydata)
{
	void *pFunc = aiCombatExitHandler_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));
	CommandQueue_Write(pQueue, &mydata, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiCombatJob_ExitFSM(Entity* e,FSMLDCombatJob* pCombatJobData);
void aiCombatJob_ExitFSM_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	FSMLDCombatJob* pCombatJobData;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &pCombatJobData, sizeof(void*));

	aiCombatJob_ExitFSM(e, pCombatJobData);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct FSMLDCombatJob FSMLDCombatJob;

void QueuedCommand_aiCombatJob_ExitFSM(CommandQueue *pQueue, Entity* e,FSMLDCombatJob* pCombatJobData)
{
	void *pFunc = aiCombatJob_ExitFSM_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &pCombatJobData, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiCombatReachedLastKnownPos(Entity* e,AIVarsBase* aib,EntityRef erTarget);
void aiCombatReachedLastKnownPos_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;
	EntityRef erTarget;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));
	CommandQueue_Read(pQueue, &erTarget, sizeof(EntityRef));

	aiCombatReachedLastKnownPos(e, aib, erTarget);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiCombatReachedLastKnownPos(CommandQueue *pQueue, Entity* e,AIVarsBase* aib,EntityRef erTarget)
{
	void *pFunc = aiCombatReachedLastKnownPos_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));
	CommandQueue_Write(pQueue, &erTarget, sizeof(EntityRef));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiConfigModRemove(Entity* be,AIVarsBase* aib,int handle);
void aiConfigModRemove_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* be;
	AIVarsBase* aib;
	int handle;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &be, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));
	CommandQueue_Read(pQueue, &handle, sizeof(int));

	aiConfigModRemove(be, aib, handle);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiConfigModRemove(CommandQueue *pQueue, Entity* be,AIVarsBase* aib,int handle)
{
	void *pFunc = aiConfigModRemove_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &be, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));
	CommandQueue_Write(pQueue, &handle, sizeof(int));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiConfigModRemoveAll(Entity* be,AIVarsBase* aib);
void aiConfigModRemoveAll_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* be;
	AIVarsBase* aib;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &be, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));

	aiConfigModRemoveAll(be, aib);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiConfigModRemoveAll(CommandQueue *pQueue, Entity* be,AIVarsBase* aib)
{
	void *pFunc = aiConfigModRemoveAll_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &be, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiFakeCombatExitHandler(Entity* e);
void aiFakeCombatExitHandler_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));

	aiFakeCombatExitHandler(e);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_aiFakeCombatExitHandler(CommandQueue *pQueue, Entity* e)
{
	void *pFunc = aiFakeCombatExitHandler_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiMovementAvoidLeft(Entity* e,AIVarsBase* aib);
void aiMovementAvoidLeft_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));

	aiMovementAvoidLeft(e, aib);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiMovementAvoidLeft(CommandQueue *pQueue, Entity* e,AIVarsBase* aib)
{
	void *pFunc = aiMovementAvoidLeft_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiMovementResetPathEx(Entity* e,AIVarsBase* aib,const char* filename,int linenumber);
void aiMovementResetPathEx_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;
	char* filename = NULL;
	int linenumber;

	CommandQueue_EnterCriticalSection(pQueue);
	estrStackCreate(&filename);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));
	CommandQueue_ReadString(pQueue, &filename);
	CommandQueue_Read(pQueue, &linenumber, sizeof(int));

	aiMovementResetPathEx(e, aib, filename, linenumber);
	estrDestroy(&filename);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiMovementResetPathEx(CommandQueue *pQueue, Entity* e,AIVarsBase* aib,const char* filename,int linenumber)
{
	void *pFunc = aiMovementResetPathEx_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));
	CommandQueue_WriteString(pQueue, filename);
	CommandQueue_Write(pQueue, &linenumber, sizeof(int));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiPowerConfigModRemoveMods(Entity* e,FSMLDAddStructModTagSet* modSet);
void aiPowerConfigModRemoveMods_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	FSMLDAddStructModTagSet* modSet;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &modSet, sizeof(void*));

	aiPowerConfigModRemoveMods(e, modSet);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct FSMLDAddStructModTagSet FSMLDAddStructModTagSet;

void QueuedCommand_aiPowerConfigModRemoveMods(CommandQueue *pQueue, Entity* e,FSMLDAddStructModTagSet* modSet)
{
	void *pFunc = aiPowerConfigModRemoveMods_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &modSet, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void aiTurnOffHealing(Entity* e,AIVarsBase* aib);
void aiTurnOffHealing_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;
	AIVarsBase* aib;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));
	CommandQueue_Read(pQueue, &aib, sizeof(void*));

	aiTurnOffHealing(e, aib);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_aiTurnOffHealing(CommandQueue *pQueue, Entity* e,AIVarsBase* aib)
{
	void *pFunc = aiTurnOffHealing_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void deleteMyData(ExprContext* context,ParseTable* pti,ExprLocalData*** localData,U64 key);
void deleteMyData_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	ExprContext* context;
	ParseTable* pti;
	ExprLocalData*** localData;
	U64 key;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &context, sizeof(void*));
	CommandQueue_Read(pQueue, &pti, sizeof(void*));
	CommandQueue_Read(pQueue, &localData, sizeof(void*));
	CommandQueue_Read(pQueue, &key, sizeof(U64));

	deleteMyData(context, pti, localData, key);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct ExprContext ExprContext;
typedef struct ParseTable ParseTable;
typedef struct ExprLocalData ExprLocalData;

void QueuedCommand_deleteMyData(CommandQueue *pQueue, ExprContext* context,ParseTable* pti,ExprLocalData*** localData,U64 key)
{
	void *pFunc = deleteMyData_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &context, sizeof(void*));
	CommandQueue_Write(pQueue, &pti, sizeof(void*));
	CommandQueue_Write(pQueue, &localData, sizeof(void*));
	CommandQueue_Write(pQueue, &key, sizeof(U64));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void efDisorientedEnd(AIVarsBase* aib);
void efDisorientedEnd_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	AIVarsBase* aib;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &aib, sizeof(void*));

	efDisorientedEnd(aib);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_efDisorientedEnd(CommandQueue *pQueue, AIVarsBase* aib)
{
	void *pFunc = efDisorientedEnd_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void efPatrolUnsetDataBit(ExprContext* context);
void efPatrolUnsetDataBit_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	ExprContext* context;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &context, sizeof(void*));

	efPatrolUnsetDataBit(context);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct ExprContext ExprContext;

void QueuedCommand_efPatrolUnsetDataBit(CommandQueue *pQueue, ExprContext* context)
{
	void *pFunc = efPatrolUnsetDataBit_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &context, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void efPatrolWaypointPassed(EntityRef ref,ExprContext* context,NavPathWaypoint* wp,int point);
void efPatrolWaypointPassed_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	EntityRef ref;
	ExprContext* context;
	NavPathWaypoint* wp;
	int point;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &ref, sizeof(EntityRef));
	CommandQueue_Read(pQueue, &context, sizeof(void*));
	CommandQueue_Read(pQueue, &wp, sizeof(void*));
	CommandQueue_Read(pQueue, &point, sizeof(int));

	efPatrolWaypointPassed(ref, context, wp, point);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct ExprContext ExprContext;
typedef struct NavPathWaypoint NavPathWaypoint;

void QueuedCommand_efPatrolWaypointPassed(CommandQueue *pQueue, EntityRef ref,ExprContext* context,NavPathWaypoint* wp,int point)
{
	void *pFunc = efPatrolWaypointPassed_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &ref, sizeof(EntityRef));
	CommandQueue_Write(pQueue, &context, sizeof(void*));
	CommandQueue_Write(pQueue, &wp, sizeof(void*));
	CommandQueue_Write(pQueue, &point, sizeof(int));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void efWanderDecreaseWanderCount(AIVarsBase* aib);
void efWanderDecreaseWanderCount_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	AIVarsBase* aib;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &aib, sizeof(void*));

	efWanderDecreaseWanderCount(aib);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct AIVarsBase AIVarsBase;

void QueuedCommand_efWanderDecreaseWanderCount(CommandQueue *pQueue, AIVarsBase* aib)
{
	void *pFunc = efWanderDecreaseWanderCount_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &aib, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void exposeLeaderStoppedPursuing(Entity* e);
void exposeLeaderStoppedPursuing_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));

	exposeLeaderStoppedPursuing(e);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_exposeLeaderStoppedPursuing(CommandQueue *pQueue, Entity* e)
{
	void *pFunc = exposeLeaderStoppedPursuing_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void exprCleanUpExternVarOverride(ExprContext* context,const char* category,const char* name,MultiVal* val);
void exprCleanUpExternVarOverride_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	ExprContext* context;
	char* category = NULL;
	char* name = NULL;
	MultiVal* val;

	CommandQueue_EnterCriticalSection(pQueue);
	estrStackCreate(&category);
	estrStackCreate(&name);

	CommandQueue_Read(pQueue, &context, sizeof(void*));
	CommandQueue_ReadString(pQueue, &category);
	CommandQueue_ReadString(pQueue, &name);
	CommandQueue_Read(pQueue, &val, sizeof(void*));

	exprCleanUpExternVarOverride(context, category, name, val);
	estrDestroy(&category);
	estrDestroy(&name);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct ExprContext ExprContext;
typedef struct MultiVal MultiVal;

void QueuedCommand_exprCleanUpExternVarOverride(CommandQueue *pQueue, ExprContext* context,const char* category,const char* name,MultiVal* val)
{
	void *pFunc = exprCleanUpExternVarOverride_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &context, sizeof(void*));
	CommandQueue_WriteString(pQueue, category);
	CommandQueue_WriteString(pQueue, name);
	CommandQueue_Write(pQueue, &val, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void leftWaitingForDespawn(Entity* e);
void leftWaitingForDespawn_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	Entity* e;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &e, sizeof(void*));

	leftWaitingForDespawn(e);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct Entity Entity;

void QueuedCommand_leftWaitingForDespawn(CommandQueue *pQueue, Entity* e)
{
	void *pFunc = leftWaitingForDespawn_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &e, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
void stopTracking(FSMContext* context,GameEvent* ev);
void stopTracking_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	FSMContext* context;
	GameEvent* ev;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &context, sizeof(void*));
	CommandQueue_Read(pQueue, &ev, sizeof(void*));

	stopTracking(context, ev);

	CommandQueue_LeaveCriticalSection(pQueue);
}
typedef struct FSMContext FSMContext;
typedef struct GameEvent GameEvent;

void QueuedCommand_stopTracking(CommandQueue *pQueue, FSMContext* context,GameEvent* ev)
{
	void *pFunc = stopTracking_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &context, sizeof(void*));
	CommandQueue_Write(pQueue, &ev, sizeof(void*));

	CommandQueue_LeaveCriticalSection(pQueue);
}
