//For more info on queued commands, look here: http://crypticwiki:8081/display/Core/AUTO_COMMAND_QUEUED
//This file is autogenerated. autogeneratednocheckin
#include "commandqueue.h"

void queueTest(int x,float y);
void queueTest_QUEUEDWRAPPER(CommandQueue *pQueue)
{
	int x;
	float y;

	CommandQueue_EnterCriticalSection(pQueue);

	CommandQueue_Read(pQueue, &x, sizeof(int));
	CommandQueue_Read(pQueue, &y, sizeof(float));

	queueTest(x, y);

	CommandQueue_LeaveCriticalSection(pQueue);
}

void QueuedCommand_queueTest(CommandQueue *pQueue, int x,float y)
{
	void *pFunc = queueTest_QUEUEDWRAPPER;
	CommandQueue_EnterCriticalSection(pQueue);
	CommandQueue_Write(pQueue, &pFunc, sizeof(void*));
	CommandQueue_Write(pQueue, &x, sizeof(int));
	CommandQueue_Write(pQueue, &y, sizeof(float));

	CommandQueue_LeaveCriticalSection(pQueue);
}
