#pragma once

/*"Controller Mirror" mode means that there is a gameserver running as a web request server, and the
controller is periodically sending it copies of the high level shard info.*/

#include "../../core/controller/pub/controllerpub.h"

